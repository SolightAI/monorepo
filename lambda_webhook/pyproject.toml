[project]
name = "solight-webhook"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]>=0.115.6",
    "redis>=5.0.7",
    "types-redis>=4.6.0",
    "httpx==0.27.2",
    "pytest>=7.0.0",
    "pytest-asyncio>=0.18.0",
]