name: aws-infra
description: AWS infra automatic deployment for AWS lambda and ECR images
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
